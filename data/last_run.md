# Last run — demo data

This file is overwritten on every real scrape.
Run `python -m scraper.run` (after `pip install -r requirements.txt && python -m playwright install chromium`)
to populate it with the actual per-company status report.
